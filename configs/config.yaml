experiment:
  seed: 20260430
  num_classes: 4
  target_classes:
    - "Stop Sign"
    - "Swiping Left"
    - "Sliding Two Fingers Down"
    - "Thumbs Up"
  frames_per_video: 16
  batch_size: 16
  learning_rate: 0.0001
  quick_test: false
  execution_mode: "gpu"

paths:
  original_raw_dir: "${RAW_ORIGINAL_JESTER_DIR}"
  full_dataset_dir: "${DATA_ROOT}/jester_uncompressed"
  dev_dataset_dir: "${DATA_ROOT}/jester_data_for_dev"
  weights_dir: "${SHAN_PROJECT_DIR}/weights"